import { styled } from "styled-components";

export const StyledILabel = styled.label`
    margin: 7px 0;
    font-family: var(--header2p);
    font-size: 20px;
    span{
        color: red;
    }
    `