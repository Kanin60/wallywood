import { styled } from "styled-components";

export const StyledInput = styled.input`
    border: none;
    border-top: 3px solid var(--clamShell);
    background-color: #fff;
    padding: 7px;
    font-family: var(--header2p);
    `